# راهنمای محیط توسعه - Enhanced RAG System

## ✅ کار انجام شده

یک کپی کامل از فولدر `enhanced_rag_system` با نام `enhanced_rag_system_dev` ایجاد شد و تمام تنظیمات برای محیط توسعه به‌روزرسانی شد.

## 📋 تغییرات اعمال شده

### 1. پورت‌ها
- **API Server**: از `8000` به `8001` تغییر یافت
- **Nginx HTTP**: از `80` به `8080` تغییر یافت  
- **Nginx HTTPS**: از `443` به `8443` تغییر یافت
- **Redis**: از `6379` به `6380` تغییر یافت

### 2. مسیرها
- مسیر اصلی: `/home/user01/qwen-api/enhanced_rag_system_dev`
- مسیر دیتابیس: `/home/user01/qwen-api/enhanced_rag_system_dev/chroma_db`
- مسیر لاگ: `/home/user01/qwen-api/enhanced_rag_system_dev/api_server.log`

### 3. فایل‌های به‌روزرسانی شده
- ✅ `api_server.py` - پورت و مسیرها
- ✅ `config/settings.py` - مسیر دیتابیس
- ✅ `ultimate_rag_system.py` - مسیرها
- ✅ `start_api.sh` - مسیرها و پیام‌ها
- ✅ `docker-compose.yml` - پورت‌ها و نام سرویس‌ها
- ✅ `search/multi_hop_retriever.py` - مسیرها
- ✅ `ultimate_rag_ui.py` - مسیرها

## 🚀 راه‌اندازی

### روش 1: استفاده از اسکریپت (پیشنهادی)
```bash
cd /home/user01/qwen-api/enhanced_rag_system_dev
./start_api.sh
```

### روش 2: اجرای مستقیم
```bash
cd /home/user01/qwen-api/enhanced_rag_system_dev
export PYTHONPATH="/home/user01/qwen-api/enhanced_rag_system_dev:$PYTHONPATH"
python api_server.py
```

## 🌐 دسترسی به API

بعد از راه‌اندازی، می‌توانید از آدرس‌های زیر استفاده کنید:

- **مستندات API**: http://localhost:8001/docs
- **بررسی سلامت**: http://localhost:8001/health
- **وضعیت سیستم**: http://localhost:8001/status
- **ReDoc**: http://localhost:8001/redoc

## 📡 API Endpoints اصلی

### Query (پرس‌وجو)
```
POST http://localhost:8001/v2/query
POST http://localhost:8001/v2/query/streaming
```

### Upload (آپلود فایل)
```
POST http://localhost:8001/upload/pdf
POST http://localhost:8001/upload/excel
POST http://localhost:8001/upload/batch
```

### Collections (مدیریت مجموعه‌ها)
```
GET    http://localhost:8001/collections
GET    http://localhost:8001/collections/{name}/info
DELETE http://localhost:8001/collections/{name}
```

### Chat Sessions (جلسات چت)
```
POST   http://localhost:8001/chat/sessions
GET    http://localhost:8001/chat/sessions/{id}
POST   http://localhost:8001/chat/sessions/{id}/messages
DELETE http://localhost:8001/chat/sessions/{id}
```

## ⚠️ نکات مهم

1. **عدم تداخل**: محیط توسعه (`enhanced_rag_system_dev`) و پروداکشن (`enhanced_rag_system`) می‌توانند **همزمان** اجرا شوند بدون هیچ تداخلی

2. **دیتابیس جداگانه**: هر محیط دیتابیس ChromaDB خودش را دارد:
   - Production: `/home/user01/qwen-api/enhanced_rag_system/chroma_db`
   - Development: `/home/user01/qwen-api/enhanced_rag_system_dev/chroma_db`

3. **تست امن**: تمام تغییرات، تست‌ها و توسعه را در محیط توسعه انجام دهید

4. **به‌روزرسانی پروداکشن**: بعد از تست موفق در محیط توسعه، تغییرات را به `enhanced_rag_system` کپی کنید

## 🐳 Docker (اختیاری)

اگر می‌خواهید با Docker اجرا کنید:

```bash
cd /home/user01/qwen-api/enhanced_rag_system_dev
docker-compose up -d
```

سرویس‌ها:
- `ultimate-rag-api-dev`: پورت 8001
- `nginx-dev`: پورت‌های 8080/8443
- `redis-dev`: پورت 6380

## ✅ تست سریع

```bash
# بررسی سلامت
curl http://localhost:8001/health

# وضعیت سیستم
curl http://localhost:8001/status

# لیست collections
curl http://localhost:8001/collections
```

## 📝 وضعیت

✅ **کامل**: محیط توسعه آماده استفاده است
✅ **تست شده**: تمام فایل‌های کلیدی به‌روزرسانی شده‌اند
✅ **مستندسازی**: فایل‌های README ایجاد شده‌اند

## 🎯 خلاصه

- **Production**: `enhanced_rag_system` روی پورت `8000`
- **Development**: `enhanced_rag_system_dev` روی پورت `8001`
- **هر دو می‌توانند همزمان اجرا شوند** ✅

---

**تاریخ ایجاد**: 2024-12-04
**وضعیت**: آماده استفاده ✅

